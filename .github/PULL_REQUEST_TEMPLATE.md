## What

<!-- What does this PR change? -->

## Why

<!-- Why is this change needed? Link the issue if there is one. -->

## Checklist

- [ ] `bun run check`, `bun run typecheck`, `bun run --cwd apps/desktop build` and `bun test` pass, and `bun run --cwd apps/desktop e2e` when the GUI changed
- [ ] The words follow the behavior: README, package READMEs, `SECURITY.md` when a trust line moved
- [ ] Every fix carries its test
- [ ] Anything visual was designed on the canvas first (the maintainer's, on request)
- [ ] PR title follows Conventional Commits (e.g. `feat(cli): ...`)
