{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "the window's permissions: the core set, and the pickers, for the page the daemon serves",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save"],
  "remote": {
    "urls": ["http://127.0.0.1:*"]
  }
}
