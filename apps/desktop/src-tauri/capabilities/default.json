{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "the window's permissions: the pickers and nothing else, for the page the daemon serves",
  "windows": ["main"],
  "permissions": ["dialog:allow-open", "dialog:allow-save"],
  "remote": {
    "urls": ["http://127.0.0.1:*"]
  }
}
